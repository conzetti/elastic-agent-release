---
fleet:
  enabled: true
  agent.grpc:
    address: <%= "agent.grpc.address" %>
  http:
      host:<%= "agent.monitoring.http.host" %>
<% if_p("fleet.certificate-authorities") do |v| %>
--certificate-authorities <%= v %> \
<% end %>